import 'mocha';

import * as chai from 'chai';
import * as chaiAsPromised from 'chai-as-promised';

chai.use(chaiAsPromised);
chai.should();

describe('SNSListnerDecorator', () => {

    it('should register in the sns router');

});